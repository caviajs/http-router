import { SCHEDULE_CONTEXT } from './schedule-constants';

describe('SCHEDULE_CONTEXT', () => {
  it('should have the appropriate value', () => {
    expect(SCHEDULE_CONTEXT).toEqual('Schedule');
  });
});
