import { Request, Response, Injectable, Controller, ControllerMetadata, HttpException } from '@caviajs/core';

@Injectable()
export class {{ name }}Controller extends Controller {

  public readonly metadata: ControllerMetadata = {
    method: 'GET',
    path: '/',
  };

  public async handle(request: Request, response: Response): Promise<string> {
    throw new HttpException(501);
  }

}
