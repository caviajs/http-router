import http from 'http';
import { Injectable, Parser, ParserMetadata, HttpException } from '@caviajs/core';

@Injectable()
export class {{ name }}Parser extends Parser {
  public readonly metadata: ParserMetadata = {
    mimeType: 'text/plain',
  };

  public async parse(buffer: Buffer, headers: http.IncomingHttpHeaders): Promise<any> {
    throw new HttpException(501);
  }
}
